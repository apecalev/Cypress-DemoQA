import { urls, LocatorElements, Buttons, Results } from "../../variables/data";
import { page, textboxfunction, elementbutton } from "../../functions/Element";


describe('Text fields with valid data', () => {
    it('should fill all text fields with valid data and submit the form', () => {
      page()
      elementbutton()
      textboxfunction()
      
    });
  });

  