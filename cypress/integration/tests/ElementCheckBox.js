import { urls, LocatorElements, Buttons, Results } from "../../variables/data";
import { page, textboxfunction, checkboxfunction, elementbutton } from "../../functions/Element";

describe('Check Box Selection Test', () => {
    it('User is able to select the checkbox for Private folder and verify the result', () => {
        page()
        elementbutton()
        checkboxfunction()
        

      
    });
  });