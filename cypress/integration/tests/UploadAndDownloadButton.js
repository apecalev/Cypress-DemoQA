import { urls, LocatorElements, Buttons, Results } from "../../variables/data";
import { page, elementbutton, uploadbuttonfunction  } from "../../functions/Element";

describe('4. User is able to upload a file', () => {
    it('should upload a file successfully and verify the result', () => {
        page()
        elementbutton()
        uploadbuttonfunction()
    });
});

