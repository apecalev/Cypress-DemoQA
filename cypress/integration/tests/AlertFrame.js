import { urls, LocatorElements, Buttons, Results } from "../../variables/data";
import { page, alertframefunction } from "../../functions/Element";

describe('Alerts - User is able to confirm action from a confirm box', () => {
    it('should display a confirm box, allow user to confirm, and verify the action result', () => {
      page()
      alertframefunction()

    });
  });